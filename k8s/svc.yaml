apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  selector:
    app: flask
    version: blue  # Switch to green later
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer
